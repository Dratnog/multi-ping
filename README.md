Launch the .\multi-ping.ps1 in a powershell session.

It will ask :
- the network address to scan.
- if you want to save the result in a file.


/!\ Limitation : The script can only scan a /24 network.
